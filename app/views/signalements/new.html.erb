<%= form_for @signalement, html: {class: 'form-horizontal'} do |form| %>
    <div class="form-group">
       <%=form.label(:cause)%> <br>
        <%=form.text_field(:cause)%>
    </div>
    <%= form.hidden_field(:comments, comment.id)%>
  <%= form.submit, "Signaler", class:"btn btn-primary"%>
<%end%>
